# 杂物社人事系统前端原型规范 v1.0

## 1. 规范目的

本规范用于统一杂物社人事系统后台端原型的页面结构、视觉语言、交互表达和验收口径。

基准页面：[组织架构](./组织基础信息表.html)

当前业务评审原型：

- [组织架构](./组织基础信息表.html)
- [岗位管理](./岗位管理.html)
- [编制管理](./编制管理.html)
- [社保信息](./社保信息.html)
- [标准工时维护](./标准工时维护.html)
- [组织、岗位、编制业务评审结论](./组织岗位编制业务评审清单.md)
- [杂物社人事系统一期 PRD](../prd/杂物社人事系统一期PRD-v1.0.md)

原型的目标是让产品、业务、研发和测试看到同一个、可以操作和验证的方案。原型不是效果图，也不是用无法点击的按钮代替尚未设计的流程。

## 2. 基本原则

1. **真实**：按钮、页码、数量、状态和示例数据必须相互一致。
2. **完整**：一期范围内的关键业务必须能从入口走到结果反馈。
3. **一致**：页面外壳、控件、状态颜色、反馈方式和弹层结构保持统一。
4. **可解释**：原型不能实现的后台动作，需要明确说明正式系统的执行规则。
5. **可访问**：鼠标、键盘和屏幕阅读器都应能识别关键控件及弹层。

## 3. 技术边界

### 3.1 原型文件

- 原型应能直接打开或通过本地静态服务访问。
- 不依赖外部 CDN、在线字体和不可控的第三方资源。
- 所有用户输入在插入页面前必须转义，避免把示例代码带入正式系统时形成安全问题。
- 不使用 `onclick` 等行内事件；统一通过脚本绑定事件。
- 不使用浏览器 `alert` 作为常规反馈；成功、失败使用消息提示，重要操作使用确认弹层。
- 静态业务评审原型中的上传文件使用代表性样例模拟结构校验、业务校验、结果反馈和日志联动，不承担真实 Excel 解析；正式前端必须按PRD读取用户实际上传的数据。
- 静态原型中的省市区数据用于演示地址识别和级联交互，正式前端必须接入完整、统一的行政区划字典，并覆盖县级市等业务取值。

### 3.2 正式前端

正式实现沿用 `hrms` 的 Vue 3、Element Plus 和现有 `MainLayout`。原型中的通用结构应映射到 Element Plus 组件，不重复开发另一套业务组件。

| 原型结构 | 正式实现建议 |
|---|---|
| 页面外壳 | `MainLayout.vue` |
| 筛选区 | `el-card` + `el-form` |
| 数据表格 | `el-table` + `el-table-column` |
| 分页 | `el-pagination` |
| 新增/编辑 | `el-dialog` + `el-form` |
| 详情 | `el-dialog` / `el-drawer` + `el-descriptions` |
| 状态 | `el-tag` |
| 消息反馈 | `ElMessage` / `ElMessageBox` |

## 4. 页面外壳

| 项目 | 标准 |
|---|---|
| 展开侧栏 | 220px |
| 收起侧栏 | 64px |
| 顶部导航 | 50px |
| 主内容边距 | 16px |
| 卡片间距 | 12px |
| 页面背景 | `#f0f2f5` |
| 侧栏背景 | `#001529` |
| 主色 | `#409eff` |

页面命名必须在浏览器标题、侧栏菜单、面包屑和页面标题中保持一致。原型侧栏只展示已生成且可访问的原型页面，不使用未实现模块作为占位菜单。

业务页面不展示“业务确认稿”“业务评审原型 vX.X”或已确认口径等文档属性。业务规则写入PRD，页面只呈现用户完成任务所需的信息、状态和操作反馈。

## 5. 标准页面结构

后台列表页按以下顺序组织：

1. 页面标题、简短说明和页面级辅助操作。
2. 只读、历史、无权限等全局状态提示。
3. 筛选卡片。
4. 主操作栏。
5. 数据表格、树表或业务主视图。
6. 真实分页。
7. 详情、编辑、确认等弹层。

组织、员工、编制等需要组织范围的页面，使用“左侧组织树 + 右侧业务列表”的双栏结构。

## 6. 视觉与尺寸

### 6.1 颜色语义

| 语义 | 颜色 | 用途 |
|---|---|---|
| 主操作 | `#409eff` | 查询、保存、新增 |
| 成功/启用 | `#67c23a` | 启用、校验通过 |
| 警告/待生效 | `#e6a23c` | 待生效、需要关注 |
| 危险 | `#f56c6c` | 删除、校验失败、不可逆操作 |
| 停用/历史 | `#909399` | 已停用、只读历史数据 |

状态颜色表达业务语义，不能为了“好看”随机分配。停用组织属于正常历史状态，默认使用灰色而不是红色。

### 6.2 间距与圆角

- 使用 4px 基础网格，常用间距为 8、12、16、20、24px。
- 默认控件高度 32px，小型控件 28px。
- 表格默认行高 48px。
- 卡片圆角 6px，弹层圆角 8px。
- 禁止在业务页面散落重复内联样式。

## 7. 组件规范

### 7.1 按钮

- 每个区域只保留一个最主要的主按钮。
- 其他按钮使用默认样式，删除或高风险操作使用危险样式。
- 按钮必须可完成演示；未设计的功能使用禁用态并给出原因。
- 图标必须来自统一图标库；不使用 Emoji 充当业务图标。
- 提交过程中显示加载状态并禁止重复提交。

### 7.2 筛选区

- 标签置于控件上方或使用统一的行内表单，不混用。
- 查询条件每行最多展示4个字段，超过4个时从下一行继续排列；查询、重置按钮不计入字段数量，并放在当前最后一行的右侧或按窄屏规则独占一行。
- “重置”需要恢复页面默认范围；如果保留组织范围，按钮名称应为“重置条件”。
- 查询后分页回到第一页。
- 搜索无结果时显示独立空状态，不显示假页码。

### 7.3 表格

- 编码、日期、状态和操作列禁止换行。
- 长名称使用省略号并提供完整内容提示。
- 操作列固定在右侧；横向滚动只发生在表格内部。
- 从其他主数据同步而来的只读列在列表中按普通正文显示，不将整列做成禁用态；进入编辑表单后，同一字段必须使用统一的灰底只读样式，与本模块可编辑字段明确区分。
- 页面数据少于一页时只显示一个页码。
- 分页序号按真实页码和每页条数计算。
- 业务评审原型必须直接设计无数据和筛选无结果状态；加载中、加载失败和无权限由正式前端的统一状态组件覆盖，可集中验证，不要求在每个静态HTML中重复设置演示入口。

### 7.4 组织树

- 搜索结果必须保留父级路径并自动展开。
- 组织树搜索统一支持组织名称、组织编码和门店编码；不存在门店编码的节点按名称和组织编码匹配。
- 层级不限制深度；名称过长时省略，并保留横向滚动或完整提示。
- 左侧组织树只展示状态为“生效中”的组织；待生效、已失效、已停用组织不进入组织树，仍可在右侧列表中查询和查看。
- 组织架构树节点不展示直属下级数、全部下级数、人员数或业务数据量。
- 正常的“生效中”节点不显示绿点或其他状态点；仅在存在需要特别表达的异常或业务状态时使用标记，并同时提供文字说明。
- 选择组织后，列表范围统一为“当前组织及全部下级”。

### 7.5 表单

- 必填项使用统一星号，错误信息放在对应字段附近或表单顶部。
- 编辑界面中不可修改的字段必须使用禁用或只读状态，并统一显示为灰色背景、次要文字和禁用光标；新增界面可填写的同一字段保持正常白底，不仅依赖文字说明表达不可修改。
- 组织负责人使用人员选择器，不使用自由文本。
- 新增组织和调整组织使用同一套可检索上级组织下拉组件，并排除自身、全部下级组织、门店、已停用和已失效组织：收起时仅展示已选组织名称；展开后在下拉面板顶部提供搜索框，候选项展示完整组织路径。搜索支持组织名称和组织编码。
- 上级组织候选路径不展示组织编码；组织树、组织列表及其他业务页面的“组织名称”字段不拼接任何编码，需要时可在独立列展示组织编码或门店编码。
- 其他需要选择门店的组织下拉（如编制的关联门店）中，候选路径里的门店名称在门店编码非空时显示为“门店名称(门店编码)”，使用英文半角括号；其他组织类型不追加组织编码。
- 组织编码唯一，创建后默认不可修改。
- 组织编码、门店编码、同一上级下的组织名称和岗位名称做唯一性或匹配校验时忽略首尾空格与大小写，但保存并回显用户输入的显示值。
- 所有模块的新增、编辑表单都不在字段下方展示唯一性、是否可修改、取值范围、计算影响等业务规则；这些规则统一记录在PRD中，界面只保留必要的字段标签、输入示例和校验反馈。
- 生效时间精确到时分秒；填写失效时间时，失效时间必须严格晚于生效时间。
- 子组织有效期必须位于上级组织有效期内；编辑上级组织或调整组织层级时，需要校验上级有效期覆盖整个下级子树。
- 门店类型才显示门店编码、门店性质、门店面积、年营业额预估、门店等级、门店类别、加盟商转出情况、改造移位续约时间、开店协同交房表状态、开闭店日期、地址、省市区和经纬度；门店编码选填、填写后全局唯一并允许修改。
- 通过取消、关闭按钮或 Esc 离开表单时直接丢弃未保存内容，不增加二次确认。

### 7.6 弹层

- 弹层必须有明确标题、关闭按钮、取消按钮和主操作按钮。
- 简单表单使用 480–680px 对话框；多分区详情使用 680–880px 对话框或抽屉。
- 弹层打开后锁定背景交互和焦点，支持 Esc 关闭。
- 重大业务动作需要展示影响范围后再次确认。

### 7.7 操作日志

- 任何编辑、调整、启用、停用或系统自动变更，都必须记录实际发生变化的全部字段。
- 说明统一使用“字段：变动前值 → 变动后值”的格式；多个字段使用分号分隔，不记录未变化字段。
- 空值统一展示为“未填写”；枚举、人员、组织等关联字段展示业务名称，不展示内部编码或ID。
- 表单提交后没有字段发生变化时，不新增操作日志。
- 批量导入的批次日志记录操作来源、导入文件、成功数、失败数和无变化数；每条成功新增或更新的业务记录另行生成对象日志。其中，更新日志必须保存全部变更字段的变更前值、变更后值、来源和所属导入文件，便于从单条数据反向追溯导入批次。
- 系统根据时间或跨模块联动自动修改数据时，操作人记录为“系统”，并同时记录所有变化字段、变更来源以及对应业务对象的单条日志。

### 7.8 自动关联档案页

- 当某类扩展档案与上游主数据一一对应时，上游对象创建后，档案列表自动出现对应记录；不再提供独立的“新增”或“删除”入口。
- 对象名称、业务编码、状态、归属路径等主数据字段由上游自动同步，在本页面只读；页面只允许编辑本模块的扩展字段。
- 详情或编辑弹层中的同步字段统一显示为灰底只读态；当数据来源容易被误解时，可在分区标题或提示中简短说明同步来源，不在每个字段下重复说明。
- 列表范围、可编辑状态和导出范围必须在对应业务规则中明确，不以“未在组织树展示”作为隐式过滤条件。

## 8. 人事领域交互规则

### 8.1 组织调整

调整上级组织时必须展示：

- 调整前和调整后的上级组织。
- 调整原因和可选附件。
- 随组织移动的下级组织数量。
- 当前组织及全部下级的在职人员数量。
- 操作日志和组织快照影响。

禁止选择自身或任一下级作为上级组织。

当前业务口径为管理员当日直接调整，保存后立即生效，不走审批、不支持预约未来生效。

### 8.2 停用组织

停用前需要按当前组织及全部层级下级检查并展示：

- 待生效或生效中的下级组织。
- 在职人员。
- 生效中的编制（只警告，不阻塞）。

存在待生效/生效中的下级组织或在职人员时禁止停用；存在生效编制时允许停用，但必须明确警告且不得联动修改编制。已停用、已失效组织均允许编辑和调整层级，保存后保持原状态；通过独立“启用”操作恢复，并校验直接上级处于生效中、组织失效时间尚未到达。

### 8.3 历史架构

- 历史模式必须有持续可见的只读提示。
- 新增、编辑、调整、启用、停用、导入和生成快照在历史模式下禁用。
- 历史模式下的组织详情只展示快照信息，不展示“停用检查”等仅服务于当前架构操作的内容。
- 支持明确返回当前架构。
- 历史列表标记每月15日自动快照、月末自动快照和手工快照，并显示创建人、说明和附件。
- 月中和月末自动快照时间分别固定为当月15日23:59:59和月末23:59:59；手工快照时间打开弹窗时由系统生成并以灰底只读状态展示。
- 调整组织和生成手工快照均支持可选附件；附件需写入对应操作日志，并允许从历史架构或操作日志查看。

### 8.4 岗位与编制

- 岗位名称全局唯一；岗位适用职等从0–20任意多选，新增和编辑使用收起状态可汇总回显、展开后可勾选多个值的下拉多选控件。
- 编辑岗位时岗位状态为灰底禁用字段，不允许在编辑表单中修改；状态统一通过列表中的启用、停用操作变更，并进行二次确认。
- 岗位定义编制占用数量，值只能为0.5或1。
- 一条编制代表一个独立席位，计划数量根据所选编制岗位的编制占用数量自动带入，不允许手工修改。
- 岗位编制占用数量变化时，正常及已停用编制的计划数量随岗位更新；已失效编制、已经到期且计划为0的临时编制保持原计划数量。所有引用该岗位的人员占用会触发实际数量和占用状态重算，并产生逐条系统日志。
- 编制列表的有效期只展示到年月日；详情、编辑及导出数据仍保留精确到时分秒的原始值。
- 编制实际数量按关联人员岗位的编制占用数量汇总。
- 编制有人员占用时仍允许修改关联门店和编制岗位；修改门店只影响编制归属，不改变员工组织、员工岗位或既有占用关系。实际数量大于0时，停用按钮直接置灰。
- 生命周期状态与占用状态分开展示，不能使用一个状态字段混合表达。
- 编制页不展示工时；月标准工时使用考勤管理下的独立页面维护。

### 8.5 批量导入

当模块同时支持导入新增和导入更新时，必须明确区分两种模式。页面操作区原则上只保留一个“批量导入”入口，在弹窗内选择“导入新增”或“导入更新”；仅支持其中一种模式时，入口名称直接表达具体动作。

#### 8.5.1 导入新增

- 使用独立的空白新增模板，模板必须列出完成新增所需的必填字段和可选字段。
- 只创建新数据；根据本模块的唯一键判定记录已存在时，该行导入失败，不自动转为更新。

#### 8.5.2 导入更新

- 不提供独立的空白更新模板。用户先按当前页面范围和查询条件“导出当前结果”（有组织树时包含当前组织范围），再在导出文件中修改可编辑字段并重新导入。
- 导出文件必须包含该模块的匹配键、必要只读识别字段和全部可更新字段，即使某些字段没有在页面列表中展示。
- 系统按模块规定的匹配键查找既有数据；匹配不到时该行失败，不通过更新文件新增数据。
- 可更新字段按导入行整行覆盖：空白单元格清空对应选填字段，必填字段仍执行必填、格式和业务规则校验。
- 不可修改列使用灰色表头，字段名后统一标注“（不可修改）”。导出文件另设“导入说明”工作表，说明匹配键、可修改列、不可修改列、空白覆盖和失败规则。
- 用户修改任一不可修改字段时，该行导入失败并明确提示字段名，不忽略改动，避免用户误认为修改已生效。

#### 8.5.3 通用导入流程与反馈

标准流程为：获取模板或导出当前结果 → 选择文件 → 结构校验 → 业务校验 → 结果预览 → 确认导入 → 结果反馈。

- 不能把“选择文件”直接等同于“导入成功”。
- 批量导入允许部分成功；正确记录正常处理，失败记录不影响其他通过校验的记录。
- 导入更新中校验通过但全部可编辑字段均未变化的记录计为“无变化”并跳过，不计入成功更新、不生成对象日志；批次反馈分别展示成功、失败和无变化数量。
- 导入完成后展示成功、失败和无变化数量；失败明细必须可下载，并给出原始行号、字段名和失败原因。
- 导入文件、批次汇总和逐条业务变更日志按第 7.7 节关联保存。

## 9. 页面状态清单

业务评审原型必须直接覆盖正常业务流程、无数据或筛选无结果；适用时还需覆盖只读历史、提交反馈和高风险操作确认。加载失败、无权限等技术异常状态可以复用统一组件，不要求在每个静态HTML中重复设置演示入口，但进入研发交付前必须按下列清单全部检查：

- 初始状态。
- 加载中。
- 正常数据。
- 无数据。
- 筛选无结果。
- 加载失败。
- 无权限。
- 只读历史。
- 提交中。
- 提交成功和失败。
- 高风险操作确认。

## 10. 响应式与支持范围

- 后台管理端以 1280px 及以上桌面宽度为主要验收范围。
- 1024–1279px 自动收起侧栏，表格在自身区域横向滚动。
- 不允许页面根节点产生横向滚动。
- 小于 900px 时允许组织树与列表上下排列，但移动端员工自助应使用独立 H5 原型，不直接压缩后台页面。

## 11. 可访问性

- 所有输入控件都有可关联的 `label`。
- 所有点击目标均使用 `button`、`a` 或具备等效键盘语义的元素。
- 保留清晰可见的键盘焦点样式。
- 图标按钮提供可读名称。
- 弹层使用正确的对话框语义和焦点管理。
- 状态不能只依靠颜色区分，必须同时显示文字。
- 表格包含表头、标题和正确的列语义。

## 12. 原型验收清单

新页面进入评审前应逐项确认：

- [ ] 页面名称在标题、菜单和面包屑中一致。
- [ ] 使用统一页面外壳、颜色、间距和控件尺寸。
- [ ] 所有可见按钮可点击或明确禁用。
- [ ] 数量、分页和示例数据一致。
- [ ] 关键业务流程可以走到结果反馈。
- [ ] 业务评审页面直接覆盖正常、空/筛选无结果，以及适用的只读、操作确认和结果反馈状态。
- [ ] 进入研发交付前，已通过统一状态组件验证加载中、加载失败和无权限状态。
- [ ] 1280px 下无异常换行，只有表格内部可横向滚动。
- [ ] 所有表单有必填、格式和业务规则校验。
- [ ] 高风险动作展示影响范围并二次确认。
- [ ] 可使用键盘完成查询、查看、编辑和关闭弹层。
- [ ] 不依赖外部网络资源。

## 13. 新增原型页面方式

1. 复制基准页面的系统外壳和设计变量。
2. 保留页面标题、筛选卡、主操作栏、表格/主视图、分页和弹层的结构顺序。
3. 替换业务数据模型、字段、列和关键流程。
4. 按第 9 节补齐页面状态。
5. 按第 12 节完成自检后再进入业务评审。
