# 组织、岗位、编制业务评审结论

> 本文由原“业务评审清单”更新而来，用于记录2026年9月2日业务讨论及会后逐项澄清结果。原型页面顶部的“已确认口径”属于评审辅助说明，不作为正式系统界面。

## 一、评审原型

1. [组织架构](./组织基础信息表.html)
2. [岗位管理](./岗位管理.html)
3. [编制管理](./编制管理.html)
4. [标准工时维护](./标准工时维护.html)
5. [合并PRD](../prd/组织岗位编制及标准工时管理PRD-v1.0.md)

### 通用日志规则

凡涉及字段变更，操作日志需逐项记录全部变化，格式为“字段：变动前值 → 变动后值”；不记录未变化字段，无实际变化时不生成日志。批量导入更新保留批次汇总，并为每条成功更新记录保存字段变更明细。

## 二、已确认结论

### 1. 组织

| 编号 | 已确认结论 |
|---|---|
| O1 | 组织类型为公司、职能、战区、大区、小区、门店；不校验类型上下级关系。 |
| O2 | 支持多个公司；公司可无父节点，其他组织必须选择父节点。 |
| O3 | 同一父节点下组织名称不能重复；组织编码全局唯一、手工填写、创建后不可修改。 |
| O4 | 门店另有独立门店编码，选填、手工填写，填写后全局唯一并允许修改，不校验固定格式。 |
| O5 | 组织负责人选填；HRBP仅在职能、战区、大区展示。HRBP查找逻辑留给工作流。 |
| O6 | 生效时间必填，失效时间选填，均精确到时分秒；开店、闭店日期选填且只精确到日。 |
| O7 | 门店性质为直营、加盟商合杰、加盟商自理；社保类型为本地社保、异地社保。 |
| O8 | 门店地址、省、市、区均选填。用户填写地址后点击按钮识别省市区，允许手工修改。 |
| O9 | GPS经纬度手工维护，暂不校验坐标系。 |
| O10 | 组织调整不走审批、不预约未来生效；管理员当天调整父节点，原因必填、附件选填，附件写入操作日志并可查看。 |
| O11 | 每月15日、月末自动生成组织快照，同时支持手工快照；手工快照附件选填，可从历史架构和操作日志查看。 |
| O12 | 状态为待生效、生效中、已失效、已停用；左侧组织树只展示生效中的组织，其他状态仅在列表中查询和查看；树节点不显示绿色点、不展示数量。 |
| O13 | 存在生效下级组织、在职人员或生效编制时禁止停用；组织不物理删除。 |
| O14 | 导入支持新增和更新，更新以组织编码唯一匹配。 |
| O15 | 新增和调整组织使用相同的可检索上级组织下拉组件：面板顶部搜索，候选项展示完整路径，选中后字段只展示组织名称；支持按组织名称或组织编码过滤。 |
| O16 | 门店只能作为子节点，不能被选择为上级组织；候选路径不展示组织编码，组织树、组织列表和其他业务页面只展示组织名称。 |

### 2. 岗位

| 编号 | 已确认结论 |
|---|---|
| P1 | 岗位字段为岗位名称、适用职等、编制占用数量、岗位状态和备注；不设置岗位编码。 |
| P2 | 岗位名称全局唯一。 |
| P3 | 一个岗位可以从0–20中任意选择多个职等，至少选择一个，不要求连续；数字越大职等越高；新增和编辑使用下拉多选控件。 |
| P4 | 编制占用数量在岗位定义中维护，只能选择0.5或1。 |
| P5 | 岗位状态为启用、停用，新增默认启用；编辑岗位时不允许修改状态，状态只能通过启用、停用操作变更；岗位不物理删除。 |
| P6 | 岗位启用、停用均需二次确认；停用后历史编制和人员继续显示原岗位，新记录不能再选择停用岗位。 |
| P7 | 适用职等、编制占用数量允许直接修改，不在岗位页校验现有员工。 |
| P8 | 修改编制占用数量后立即重算相关编制实际数量，超出计划数量时标记超编待处理。 |
| P9 | 兼职、实习生为独立岗位。 |
| P10 | 低占高不在岗位页和编制编辑页校验，留到人员流程处理。 |

### 3. 编制

| 编号 | 已确认结论 |
|---|---|
| H1 | 只有门店组织维护编制；一条记录代表一个独立编制席位。 |
| H2 | 编制编码由系统生成，格式为HC+8位流水号。 |
| H3 | 每条编制的计划数量根据所选编制岗位的编制占用数量自动带入，不允许用户手工修改。 |
| H4 | 实际数量为关联人员岗位的编制占用数量之和，一条编制可关联两名各占0.5的人员。 |
| H5 | 人员岗位与编制岗位相互独立；人员关联结果只读展示，编制页不提供手工关联或解绑。 |
| H6 | 编制类型只有正式和临时；创建后不允许修改。正式编制长期有效，临时编制精确到开始、结束时分秒。 |
| H7 | 生命周期为待生效、生效中、已失效、已停用。 |
| H8 | 临时编制到期无人时计划数量改0并失效；有人时计划数量改0、保持生效中并标记超编待处理，人员释放后自动失效。 |
| H9 | 占用状态只按计划数量与实际数量比较：空编、部分占用、满编、超编待处理。已失效、已停用显示“—”。 |
| H10 | 无人占用时可以修改门店和岗位并记录日志；有人占用时禁止修改。 |
| H11 | 临时编制生效后开始时间不可修改；有人占用时只允许延长结束时间。 |
| H12 | 有人占用时禁止停用；编制不物理删除。 |
| H13 | 正式、临时编制都支持单条新增、编辑、停用、导入新增和导入更新。 |
| H14 | 导入更新必须填写编制编码并以其唯一匹配；文件中未出现的编制不受影响。 |
| H15 | 取消旧原型的月份汇总、长期调整、审批记录、编制时间轴和工时展示，改为独立编制变更日志。 |
| H16 | 缺编、招聘汇总后续在报表模块单独建设。 |
| H17 | 编制查询条件和查询结果均展示门店编码；查询条件每行最多4个字段，超出后换行。 |
| H18 | 编制列表中的有效期只展示到年月日，详情、编辑和导出仍保留时分秒。 |
| H19 | 编制新增、编辑时，关联门店使用可检索组织下拉；仅展示门店类型，候选项展示完整组织路径，选中后只回填门店名称。 |

### 4. 标准工时

| 编号 | 已确认结论 |
|---|---|
| T1 | 新增“标准工时维护”页面，放在考勤管理菜单下。 |
| T2 | 每个月只有一条全公司统一标准工时，不区分门店、岗位或员工。 |
| T3 | 标准工时为非负数，最多保留一位小数。 |
| T4 | 只支持单条新增和编辑，不支持导入、删除。 |
| T5 | 当前允许修改历史月份，需记录日志；关账后是否锁定留到考勤模块讨论。 |
| T6 | 编制页面不展示工时，超工时申请放到考勤排班模块。 |
| T7 | 标准工时列表只提供年份查询条件。 |

## 三、明确延期事项

以下事项不得在当前原型或PRD中默认为已确认：

1. HRBP在工作流中的向上查找和多人命中规则。
2. 人员岗位、员工职等、编制岗位之间的低占高校验。
3. 员工管理是否提供手工编制关联、解绑入口。
4. 入转调离流程占用、释放编制的时点。
5. 三期、产假、长病假员工释放编制和返岗规则。
6. 缺编、招聘等报表口径。
7. 考勤关账后标准工时锁定规则。
8. 标准工时与排班可用工时的月初固化规则。

## 四、原型验收重点

- 组织树只展示生效中的组织，节点没有绿色点、不展示节点数量，多家公司能够并列展示。
- 组织HRBP字段、门店扩展字段按类型正确显示。
- 地址识别后省市区可以手工修改。
- 岗位适用职等支持0–20任意多选，编制占用数量只有0.5和1。
- 编制列表一条编制一行，编码、类型、有效期、计划/实际数量和两组状态清晰分开。
- 两名0.5人员能够共同占满一条计划数量为1的编制。
- 临时编制到期有人、无人两种状态流转都能演示。
- 标准工时月份唯一、数值一位小数、历史编辑二次确认。
